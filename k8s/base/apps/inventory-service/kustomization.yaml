apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps

namePrefix: inventory-

commonLabels:
  app: inventory-service

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
