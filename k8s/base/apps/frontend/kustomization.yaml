apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps

namePrefix: frontend-

commonLabels:
  app: frontend

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
