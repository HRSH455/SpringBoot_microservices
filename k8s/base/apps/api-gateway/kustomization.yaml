apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps

namePrefix: api-gateway-

commonLabels:
  app: api-gateway

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
